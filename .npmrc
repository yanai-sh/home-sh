# Install native optional binaries for the host (esbuild, oxlint, etc.).
# On Windows ARM64, also allow x64 so @cloudflare/workerd-windows-64 can install when using x64 Node.
supportedArchitectures.os=win32,darwin,linux
supportedArchitectures.cpu=arm64,x64
