{
  "name": "@yanai-sh/site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "velite": "velite build",
    "wrangler-types": "rm -f ./src/worker-configuration.d.ts && wrangler types ./src/worker-configuration.d.ts && tsx ./scripts/patch-worker-configuration-env.ts",
    "dev": "pnpm velite && vite dev",
    "build": "pnpm velite && pnpm wrangler-types && vite build",
    "check": "pnpm velite && pnpm wrangler-types && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "fix": "pnpm check",
    "typecheck": "pnpm check",
    "test": "vitest run --config vitest.config.ts",
    "verify": "pnpm velite && pnpm wrangler-types && pnpm check && vitest run --config vitest.config.ts && vite build",
    "preview": "wrangler dev --config wrangler.jsonc --port 4321 --ip ::",
    "smoke": "playwright test --config=tests/smoke/playwright.config.ts",
    "version-upload": "wrangler versions upload --config wrangler.jsonc",
    "version-deploy": "wrangler versions deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource-variable/space-grotesk": "^5.2.10",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@sveltejs/kit": "^2.65.0",
    "@tailwindcss/vite": "^4.3.1",
    "svelte": "^5.56.3",
    "tailwindcss": "^4.3.1",
    "velite": "^0.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1",
    "@sveltejs/adapter-cloudflare": "^7.2.8",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "playwright": "^1.60.0",
    "svelte-check": "^4.3.0",
    "@types/node": "^25.9.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.0",
    "wrangler": "^4.100.0"
  }
}
