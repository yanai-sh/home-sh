# SplashCanvas (archived lab)

Trick-room navigation prototype — horizontal ring + Contact south pole, full-viewport cells.

**Production `/` uses `SplashDeck`** (flyout carousel). This stack remains at **`/labs/splash-canvas`** for experiments and smoke comparison.

Shared panes live in `../deck/` (`SplashDeckPane`, `ResumePaneViewer`, etc.).
