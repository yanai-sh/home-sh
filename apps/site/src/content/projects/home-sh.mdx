---
slug: home-sh
title: home-sh
description: This site — Hono SSR on Cloudflare Workers with Rust/WASM field, search, and telemetry.
category: Personal Project
order: 20
featured: true
repo: yanai-sh/home-sh
problem: >-
  A portfolio should prove engineering ability by existing, not by claiming it —
  but template sites and heavy frameworks demonstrate nothing and load slowly.
approach: >-
  An edge-rendered Hono app on Cloudflare Workers with Rust/WASM modules — a
  canvas-rendered systems field and Nucleo fuzzy search over the resume — plus
  a D1-backed telemetry pipeline. No client framework; CSS and a single module
  script carry the interaction.
outcome: >-
  Sub-second loads from the edge, a background that is computed rather than
  decorated, and a deploy pipeline (staging/production Workers, SemVer tags,
  smoke tests) that mirrors how I ship real services.
tech:
  - Hono
  - Cloudflare Workers
  - Rust
  - WebAssembly
  - Vite
platforms:
  - web
---

The source of yanai.sh itself: edge SSR, Rust/WASM modules, and a two-branch deploy pipeline.
