# Host toolchains: aarch64-pc-windows-msvc, aarch64-apple-darwin, aarch64-unknown-linux-gnu, etc.
# WASM artifacts always target wasm32-unknown-unknown (browser).

[target.wasm32-unknown-unknown]
rustflags = ["-C", "target-feature=+bulk-memory,+mutable-globals,+sign-ext"]
