# Optional SOPS rules for ad-hoc encrypted files (not used by the canonical
# infra model — see infra/README.md). Repo secrets live in GitHub Actions +
# gitignored local JSON, not committed ciphertext.

creation_rules:
  - path_regex: \.enc\.(json|yaml|env)$
    age: age1hqw548k8h2j5qe8am6yreacz93rrwxpngy302yr7yfrc0z4prppq3e5xjm
